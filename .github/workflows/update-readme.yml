name: Update README

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: edisonlee55/github-activity-readme@debug
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
